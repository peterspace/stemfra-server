# Work log — 2026-07-11

## N1 — Booking-comms reliability core ✅ DONE (verified end-to-end)

The first phase of the Mindbody-gap plan (EMAIL_SMS_CATALOG.md). Everything on
the Case 9 unified email base; all senders best-effort (never fail the caller).

**Server (`stemfra_server`):**
- `templates/transactionalEmails.js` — 4 new builders: `bookingReminder`,
  `bookingCancelled`, `bookingRescheduled`, `ownerBookingNotification`
  (new|cancelled variants, CTA → CMS /bookings).
- `lib/bookingEmails.js` (NEW) — `loadBookingContext(bookingId)` (one query:
  booking + customer + site + company name + owner email + theme logo + the
  location section's public email, luxon date labels in site tz) + senders
  `sendCancellationEmails` / `sendRescheduleEmails` / `sendOwnerNewBookingEmail`
  / `sendReminderEmail`. Owner-notify pre-N2 toggle:
  `site_theme_settings.metadata.notify_owner_bookings !== false`.
- `lib/bookingReminderSweeper.js` (NEW) — every 5 min, confirmed bookings on
  LIVE sites starting within 24h, `reminder_24h_sent_at` null; **stamp-first
  claim** (update … is-null … select) so a crash can't double-email. 2h slot
  reserved for SMS (N3). Started from index.js; skips start if Gmail env missing.
- `controllers/bookingController.js` — owner new-booking notify after all 3
  booking paths (single, class, group).
- `controllers/siteMembersController.js` — member self-cancel → cancellation
  emails; member self-reschedule → reschedule email.
- `routes/cms/bookings.js` (NEW) — `POST /api/cms/bookings/notify`
  {siteId, bookingId, event: cancelled|rescheduled, oldStartsAt?}. requireCmsAuth
  + verifySiteOwnership + booking-belongs-to-site check → sends the customer
  email after the CMS's client-side mutation committed. Mounted in index.js.
- `controllers/sitePaymentsController.js` — 💳 `receipt_email` on booking
  PaymentIntents (Stripe only sends receipts in LIVE mode → dormant until
  verification completes, no extra gate needed).
- `routes/devPreview.js` — 4 new previews: /booking-reminder,
  /booking-cancelled, /booking-rescheduled, /owner-new-booking.

**CMS (`stemfra_cms`):**
- `lib/bookingNotify.ts` (NEW) — fire-and-forget authed fetch to the notify
  endpoint.
- `lib/queries.ts` — `useUpdateBooking` fires notify when `patch.status ===
  'cancelled'`; `useRescheduleBooking` gained optional `siteId`/`oldStartsAt`
  and fires notify (replaces the old Phase-6.1 n8n TODO).
- `components/bookings/RescheduleDialog.tsx` — passes siteId + the old starts_at.

**Templates:** `BookingForm` passes the customer email with `createPaymentIntent`;
crossfit `lib/booking.ts` + `BookPage` thread it to the server (`customerEmail`
→ receipt_email).

**Verified:** seeded a confirmed booking <24h out on forge-and-bell →
`sweepOnce()` sent a real reminder + claimed the stamp; notify endpoint e2e with
a real owner JWT (minted via admin magiclink token_hash — the fixture sites are
all owned by peechizzy@ now, so davis@ was correctly 403'd): cancelled →
`{client:true, owner:false}` (owner echo correctly suppressed for
business-cancelled), rescheduled → `{client:true}`, cross-tenant bookingId →
denied, bad event → 400. All 4 preview routes render with correct content.
Test booking + customer deleted. CMS + crossfit typecheck clean.

**Note:** :4000 had died (nodemon exit during an earlier session boundary) —
restarted per the keep-server-on convention.

## NEXT

- **N2** — CMS Notifications settings page, site_customers prefs (+sms_opt_in,
  birthdate), unsubscribe, System A invoice/dunning emails (provider-agnostic),
  class-session cancel tool, resend-confirmation + email-schedule tools,
  **team-member email field + per-member "copy on their bookings" toggle**
  (site_team_members has no email column today — additive migration).
- **N5 unblock**: Peter is opening the Resend account (mail.stemfra.com + API
  key) — DNS via CF + smtp.resend.com behind an env flag when ready.
