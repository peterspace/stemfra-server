# Work log — 2026-07-13

## Resend email setup + N5 transport cutover ✅ DONE (verified delivered)

Continues from N1 (WORK_2026-07-11). Goal: get real transactional email sending
through a proper ESP instead of Gmail SMTP.

### 1. `mail.stemfra.com` verified for sending (Resend)
- Peter created the Resend account + `RESEND_API_KEY` (sending-only, then briefly
  full-access to let Claude add the domain; left full-access per Peter).
- Domain `mail.stemfra.com`, region **us-east-1** (Virginia), id
  `e29b9c82-e13f-43a3-9625-ce2a8424e098`.
- Claude pulled the exact DNS records via the Resend API and wrote them to the
  **stemfra.com Cloudflare zone** via our token (already had `DNS:Edit` — no new
  permission): `resend._domainkey.mail` TXT (DKIM), `send.mail` MX
  (`feedback-smtp.us-east-1.amazonses.com`), `send.mail` TXT (SPF), plus a
  monitoring `_dmarc.mail` TXT. Triggered verify → **verified** (~40s).

### 2. Root-domain DMARC (fixes the CF "block fake emails" warning)
- The warning was about the **root `stemfra.com`** (Google Workspace mail), which
  had SPF+DKIM but no apex DMARC. Added `_dmarc.stemfra.com` TXT
  `v=DMARC1; p=none; rua=...; fo=1` (monitoring — zero delivery impact). Peter
  then enabled **Cloudflare DMARC Management** (adds their reporting RUA + a
  dashboard). Tighten p=none→quarantine/reject later once reports confirm all
  legit senders align.

### 3. N5 transport cutover — all transactional email now via Resend
- **`lib/mailer.js`** (NEW) — the single `sendMail({fromName, to, replyTo,
  subject, text, html})`. Routes Resend HTTP API vs Gmail SMTP by
  `EMAIL_PROVIDER` (`resend`|`gmail`), fallback to whichever is configured.
  Callers pass a display NAME; the mailer picks the from-address
  (`notifications@mail.stemfra.com` for Resend). `.env`: `EMAIL_PROVIDER=resend`
  + `RESEND_FROM_ADDRESS`.
- **Refactored all 7 send sites** off their own nodemailer transporters:
  `lib/bookingEmails.js` (N1 reminders/cancel/reschedule/owner), `bookingController`
  (single/visit/class confirmations), `siteFormController` (lead),
  `siteChatController` (chat lead), `contactController` (marketing ×2),
  `stripeWebhookController` (orphan alert), `cms/assistantController` (Stacy
  handoff). `bookingReminderSweeper` start guard now uses `activeProvider()`.
- **`lib/cloudflareDns.js`** (NEW, answers Peter's Q) — reusable idempotent
  `upsertDnsRecord`/`upsertDnsRecords`/`deleteDnsRecord`/`listDnsRecords` for our
  own zones (proxied:false default). Replaces the one-off `node -e` DNS scripts;
  reuse for any future ESP/verification/subdomain record.
- **Verified:** branded test (Case 9 base) via `sendMail` → Resend
  `last_event: **delivered**` to peter.space.io@gmail.com (inbox, SPF+DKIM
  aligned). All 10 changed files `node --check` clean; server reloaded healthy,
  reminder sweeper restarted recognizing Resend.

### Custom professional emails for businesses — deprioritized (Peter's call)
Since reply-to already routes to each business's own email, per-business mailboxes
(Titan) aren't urgent. Cloudflare Email Routing (Cases 7+11) already covers
receive-forwarding if a business wants `support@theirdomain.com`.

## N2 Slice 1 — team-member email + "copy on their bookings" ✅ DONE (verified)

The concrete piece scoped from Peter's staff-facing question ("copy the teacher").
- **Schema (additive):** `site_team_members.email TEXT NULL` +
  `notify_bookings BOOLEAN NOT NULL DEFAULT false` (migration
  `team_member_email_notify`); `database.types.ts` hand-patched (Row/Insert/Update);
  logged as additive change (13) in platform CLAUDE.md.
- **Server:** `lib/bookingEmails.js` `loadBookingContext` now joins
  `team:site_team_members(name, email, notify_bookings)` → exposes
  `teamMemberName` + `teamMemberEmail` (the email is ONLY set when the member opted
  in: `notify_bookings && email`). New `notifyTeamMember(c, event)` copies the
  assigned member on NEW + CANCELLED bookings (reuses `ownerBookingNotification`);
  wired into `sendOwnerNewBookingEmail` (fires independent of the owner's own
  notify setting) + `sendCancellationEmails`.
- **CMS:** `components/team/TeamMemberCard.tsx` gained an Email field (private —
  "not shown on your site") + a "Copy on their bookings" checkbox (disabled until an
  email is entered); `CardForm`/`memberToForm`/dirty/save patch extended;
  `TeamMemberUpdatePatch` in `queries.ts` +email/+notify_bookings.
- **Pre-existing bug fixed (blocked the walk):** `TeamListPage` used the
  non-null-safe `ctx?.site.id` (crashes when the active site is transiently null —
  every other CMS page uses `ctx?.site?.id`). Fixed to `?.site?.id`.
- **Verified:** repointed forge-and-bell → Noa's contact (fixture logins own no
  sites — the demo-fleet decoupling), expanded Davis Tate → Email field + toggle
  render with correct hints, filled + enabled + Save → DB persisted
  `{email, notify_bookings:true}`; seeded a booking for Davis →
  `loadBookingContext` resolved his email → `sendOwnerNewBookingEmail` → **coach
  copy delivered** (Resend). All test state reverted (Davis email/notify nulled,
  forge owner restored, booking+customer deleted). CMS typecheck clean.

## N2 Slice 2 — one-click CMS access from owner emails (magic links) ✅ DONE (verified)

Peter: owner notification emails should let the owner click straight into the CMS
(e.g. cms.stemfra.com/bookings) WITHOUT re-entering their password.
- **`lib/cmsMagicLink.js`** (NEW, reusable) — `cmsMagicLink(authUserId, path)`:
  resolves the user's real auth email via `auth.admin.getUserById` → Supabase
  `auth.admin.generateLink({type:'magiclink', redirectTo: CMS_URL+path})` → returns
  the `action_link` (the `/auth/v1/verify?token=…&redirect_to=…` URL). **Single-use +
  short-lived** (project OTP expiry ~1h — same as password resets); returns null on
  failure → caller falls back to the plain CMS URL (never a lockout). Owner-only
  (customers aren't CMS users).
- **Wired into all owner-facing CTAs**: `ownerBookingNotification` (new + cancelled →
  /bookings, via `bookingEmails.js`; `loadBookingContext` now also loads the owner's
  `auth_user_id`), `ownerLeadNotification` (contact-form lead → /leads,
  siteFormController), `ownerChatLeadNotification` (chat lead → /leads,
  siteChatController). Each builder gained a `dashboardUrl` param that overrides the
  plain CTA when a magic link is available.
- **Verified:** generated a real link for a fixture owner → valid Supabase verify URL
  with `redirect_to=https://cms.stemfra.com/bookings`; the rendered email CTA href IS
  the magic link (HTML-escaped `&amp;`, correct); fallback CTA renders the plain URL
  when no link. Syntax clean; server reloaded healthy.
- **Security note (surfaced to Peter):** each such email carries a time-limited,
  single-use login link — the standard SaaS pattern (same mechanism as password
  resets), chosen over a long-lived custom token precisely because it expires and
  can't be reused.
- **⚠️ PETER ACTION (prod + dev):** add the CMS origin to Supabase Auth → URL
  Configuration → **Redirect URLs**: `https://cms.stemfra.com/**` (prod) and
  `http://localhost:5180/**` (dev). Without it Supabase redirects the magic link to
  the Site URL instead of the /bookings|/leads deep link (auth still succeeds; only
  the landing page falls back). Same setup he did for the crossfit member magic links.
- Same helper is ready for the Stacy handoff email + any future owner CTA.

## N2 Slice 3 — CMS Notifications settings page (per-event toggles) ✅ DONE (verified)

Peter chose **per-event toggles**. Owner now controls every notification event.
- **`lib/notifyPrefs.js`** (NEW) — `resolveNotifyPrefs(metadata)` +
  `getSiteNotifyPrefs(siteId)`. Reads `site_theme_settings.metadata.notifications`
  (flat event→bool map; absent/true = ON, opt-out model). Back-compat: the pre-N2
  hidden `notify_owner_bookings` flag is the fallback for the two booking events
  until the owner saves the new page. Events: owner_new_booking / owner_cancellation
  / owner_reschedule / owner_lead / owner_chat_lead / customer_reminder.
- **Server honors every flag**: `bookingEmails.js` `loadBookingContext` exposes
  `notify` (replaces the single `ownerNotifyEnabled`); sendOwnerNewBookingEmail →
  owner_new_booking, sendCancellationEmails → owner_cancellation, sendReminderEmail →
  customer_reminder. **NEW owner-reschedule notification** added to
  sendRescheduleEmails (gated owner_reschedule) — `ownerBookingNotification` extended
  with an `event:'rescheduled'` variant (Was → Now rows). Lead controllers
  (siteFormController owner_lead, siteChatController owner_chat_lead) gate on
  `getSiteNotifyPrefs`. Team-member copy stays independent of owner prefs.
- **CMS**: new **Settings → Notifications** group (`settingsSections.ts` +
  SettingsPage + sectionIcons) with `components/settings/NotificationsSection.tsx` —
  two groups ("Emails you receive" ×5, "Emails sent to your customers" ×1) of
  switch toggles, dirty-tracked Save, writes `metadata.notifications` + clears the
  legacy flag. Mirrors the server resolver client-side.
- **Bug caught + fixed during the walk**: the toggle handler read `cur` from a
  stale closure, so two rapid toggles clobbered each other (only the last stuck).
  Switched to a functional `setDraft(prev => …)` update — verified two same-tick
  toggles now both persist.
- **Verified live** (forge → Noa temp-repoint): page renders both groups + 6
  toggles all-on by default; toggled Cancellation + Appointment reminders off →
  Save → DB `notifications` persisted exactly (`owner_cancellation:false`,
  `customer_reminder:false`, rest true) → server resolver reads them correctly.
  All test state reverted; CMS typecheck + server syntax clean; zero console errors.

## N2 Slice 4 — customer preferences + unsubscribe ✅ DONE (verified)

- **Schema (additive):** `site_customers` += `email_opt_out` + `sms_opt_in` +
  `birthdate` (migration `site_customers_email_prefs`; types hand-patched). email
  prefs used now; sms/birthdate are N3/N4 groundwork.
- **`lib/emailTokens.js`** (NEW) — stateless **signed** unsubscribe tokens
  (`<customerId>.<HMAC>`, no DB column), timing-safe verify + `unsubscribeUrl()`.
- **Public endpoint** `controllers/emailPrefsController.js` + `routes/emailPrefs.js`
  mounted `/api/site-emails`: `GET /unsubscribe?token=` → flips `email_opt_out=true`
  + a branded confirmation page with a Resubscribe link; `GET /resubscribe?token=`
  → back on. No auth (token-gated). Invalid token → 400.
- **`templates/baseEmail.js`** — optional `unsubscribeUrl` → footer "Unsubscribe
  from these emails" link.
- **Wiring:** `loadBookingContext` loads `customerId` + `customerOptedOut`;
  `sendReminderEmail` SKIPS opted-out customers and carries the unsubscribe link.
  Transactional confirmations (booking/reschedule/cancel notices) deliberately do
  NOT carry unsubscribe — they confirm an action the customer took.
- **Verified:** endpoint e2e (invalid→400, unsubscribe→opt_out=true + page renders,
  resubscribe→false); reminder returns false when opted-out, true + link present
  when opted-in (real send delivered); preview shows the footer link. Test data
  cleaned up. Server + CMS type shapes clean.
- **Note:** unsubscribe URL uses `PUBLIC_BASE_URL` (already required for Twilio);
  set in prod. Minor follow-up: a CMS indicator of a customer's opt-out status.

## N2 Slice 3 — per-event notification settings page ✅ DONE
- Server: `lib/notifyPrefs.js` `resolveNotifyPrefs(metadata)` + `getSiteNotifyPrefs(siteId)`
  — reads `site_theme_settings.metadata.notifications` (flat event→bool, opt-out
  model, legacy `notify_owner_bookings` fallback for the 2 booking events).
  `loadBookingContext` exposes `c.notify`; senders gate on it
  (owner_new_booking / owner_cancellation / owner_reschedule [NEW owner reschedule
  notification added] / customer_reminder); lead controllers gate owner_lead /
  owner_chat_lead via `getSiteNotifyPrefs`. `ownerBookingNotification` extended for
  event `rescheduled`.
- CMS: new **Settings → Notifications** group + `NotificationsSection.tsx` (6
  per-event toggle switches, 2 groups: "Emails you receive" / "Emails sent to your
  customers"); registered in settingsSections + SettingsPage + sectionIcons.
  Typecheck clean. Verified resolver (defaults on / legacy off / per-event override).

## N2 Slice 4 — customer prefs + unsubscribe ✅ DONE (verified)
- Schema (additive): `site_customers.email_opt_out` (drives unsubscribe;
  suppresses reminders + future lifecycle, NOT transactional confirmations) +
  `sms_opt_in` + `birthdate` (groundwork for N3/N4).
- `lib/emailTokens.js` — signed, stateless unsubscribe tokens (HMAC, no DB column).
  `controllers/emailPrefsController.js` + `routes/emailPrefs.js` (public
  `GET /api/site-emails/{unsubscribe,resubscribe}?token=` → branded HTML page).
  `baseEmail.js` footer gained an optional `unsubscribeUrl` link; the reminder
  email includes it + `sendReminderEmail` skips opted-out customers.
- Verified: valid token → opt-out flips + "unsubscribed" page; bad signature
  rejected; cleaned up.

## N2 Slice 5 — booking-side owner tools ✅ DONE (verified)
- **Resend confirmation:** `resendConfirmation(bookingId)` (class vs appointment
  by `class_session_id`) + `POST /api/cms/bookings/resend-confirmation` +
  BookingDetailModal "Resend confirmation" button (shown when the customer has an
  email).
- **Class-session cancel + notify:** `POST /api/cms/bookings/cancel-class-session`
  (cancels the session + every confirmed enrolled booking + emails each client via
  the tested `sendCancellationEmails`). CMS SchedulePage now routes cancellation of
  a session **with enrollments** through this tool (with a confirm dialog); empty
  sessions / restores stay a client-side status flip.
- Verified: resendConfirmation → sent; class cancel → session + booking cancelled,
  client notified; cleaned up. CMS typecheck clean.

## Fixture ownership — DO NOT re-link (correction)
Briefly re-pointed the 4 fixture sites to their per-brand contacts to "unblock dev
testing" — **reverted at Peter's direction.** Convention (Peter's deliberate call,
documented in [[project-demo-sites]] + now the platform CLAUDE.md CMS-logins note):
the 4 fixtures + 9 demos are ALL owned by `peechizzy@gmail.com` (contact
`8b3e5a87…`), kept off `@stemfra.com`; **test via a staff `@stemfra.com` login**
(staff RLS = all sites) or peechizzy directly. The per-brand login table is
historical. Lesson: check the demo-sites convention before touching
`sites.owner_contact_id`.

## N2 Slice 6 — System A billing emails ✅ DONE (verified)
Stemfra billing its BUSINESS customers (build fee + monthly hosting). Provider-
agnostic, Payoneer-first, easily adaptable (Peter's call: stay on Payoneer for now).
- `templates/transactionalEmails.js`: `platformInvoice` + `platformReceipt` (Stemfra
  brand, line-items table, CTA → CMS /billing via magic link).
- `lib/billingEmails.js`: `loadChargeContext(chargeId)` (charge + sub + site + owner
  email/auth) + `sendInvoiceEmail` (payment request) + `sendReceiptEmail`. Payment
  copy from `PAY_INSTRUCTIONS` keyed by charge.provider (payoneer defined; others use
  a generic default) — switching Airwallex/Stripe = a copy edit here, nothing else.
- Hooks (best-effort, lazy-required): `lib/billing/index.js` `markRequested` →
  invoice email, `markPaid` → receipt. So every caller (admin billing UI, sweeper,
  future webhook) triggers the right email.
- Preview: `/dev/preview/platform-invoice` + `/platform-receipt` (both 200).
- Verified: seeded a charge on the existing sub → context resolved (Forge & Bell /
  owner / $99 / line items / provider), both builders render (Total due, How to pay,
  Amount paid); cleaned up. Send path via Resend already proven.
- REMAINING billing email: **dunning** (payment failed / overdue) — fire from the
  billing-cycle sweeper when a charge passes its due date unpaid. Next.

## Billing email STYLE — PDF always attached (Peter's decision, refined from N2 Slice 6)
Peter compared Render/Stripe (rich inline), Atlassian, and Google Workspace → chose
the **Atlassian/Google style: a LEAN email + the invoice/receipt ATTACHED as a PDF**
(the detail lives in the PDF, not the email body).
- `lib/invoicePdf.js` refactored: drawing extracted into `drawInvoice(doc, opts)`;
  `streamInvoicePdf(res, opts)` (CMS View/Download, unchanged) + NEW
  `renderInvoicePdfBuffer(opts) → Promise<Buffer>` for attachments. Reuses the SAME
  branded PDF the CMS "Download PDF" already produces (no second design).
- `lib/mailer.js` gained `attachments` support (Resend: base64 content; Gmail:
  Buffer — normalized in one place).
- `lib/billingEmails.js`: `loadChargeContext` now also loads the full owner contact
  (for the PDF bill-to) + `invoiceAttachment(c)`; invoice/dunning attach `INV-*.pdf`,
  receipt attaches `RECEIPT-*.pdf`. Builders (`platformInvoice/Dunning/Receipt`)
  slimmed to a compact info box + "attached as a PDF" note + magic-link CTA.
- Verified: PDF buffer is a valid 13KB %PDF; lean email renders; **a real invoice
  email with the PDF attached delivered to peechizzy@gmail.com** (Resend, from
  `Stemfra Billing <notifications@mail.stemfra.com>`). Preview:
  `/dev/preview/platform-invoice|platform-dunning|platform-receipt`.

## Billing email footer links (Figma pattern, Peter 2026-07-13)
`baseEmail.js` footer gained an optional `footerLinks` row; the System-A billing
builders pass `BILLING_LINKS` = Help (/faq) · Terms (/terms) · Privacy (/privacy) ·
Cancellation & refunds (/refund) — all pages that already exist on stemfra_client.
Scoped to billing emails only (tenant booking emails don't carry Stemfra legal
links). "Manage billing" = the existing magic-link CTA. Now matches Figma's trust
footer. Verified in the preview.

## N4 Slice 1 — lifecycle sweeper + first-visit follow-up ✅ DONE (verified)
- `lib/lifecycleSweeper.js` (NEW, started in index.js, every 12h): finds bookings
  whose FIRST visit happened ~1 day ago (starts_at 1–2 days back, not cancelled, on
  a LIVE site, no earlier non-cancelled booking for that customer) → sends the
  first-visit follow-up. Milestones stamped per-customer-once, so idempotent.
- `lib/lifecycleEmails.js` (NEW): `sendFirstVisitFollowup(bookingId)` — reuses
  `loadBookingContext` (now exposes `bookingUrl` = the tenant's /book), honors
  `email_opt_out`, gates on per-site `metadata.lifecycle_emails.first_visit`
  (default on — the CMS "Emails" page writes it later), stamps
  `site_customers.metadata.lifecycle.first_visit_sent_at`.
- `templates/transactionalEmails.js`: `firstVisitFollowup` builder (tenant brand,
  thanks + "Book your next visit" CTA + unsubscribe). Preview:
  `/dev/preview/first-visit-followup`.
- Verified: seeded a first-visit booking 1.5d ago on live forge → sweep sent 1 +
  stamped; second sweep sent 0 (idempotent). Real email delivered; renders correctly.
- NEXT N4 slices: win-back (lapsed) · post-appointment review ask · birthday
  (birthdate captured in N2) · first-visit anniversary · no-show note · then the
  CMS "Emails" page (toggles → metadata.lifecycle_emails).

## Case 9 email restyle + Payoneer Option B (Peter, 2026-07-13)
- **Email alignment model (Google-style, all Case 9 emails):** BODY (paragraphs +
  note) = LEFT (`renderEmail` default `bodyAlign:'left'`); HEADING = centered;
  FOOTER = centered; logo = centered. (First pass wrongly left-aligned the heading +
  footer — corrected to centered per Peter; only the body is left.) Blue hyperlinks
  everywhere: new `T.link = #1a73e8` on footer links, unsubscribe, "powered by
  Stemfra", + the security-line mailtos. Verified on the invoice + a tenant booking
  confirmation.
- **Payoneer Option B — "Pay now" in our email** (Peter confirmed; Payoneer has no
  API so it's manual, and we embed the pay LINK rather than have Payoneer email
  separately): `loadChargeContext` exposes `payUrl` = `external_ref` when it's a URL
  (staff paste the Payoneer payment link at "mark requested"). `platformInvoice`/
  `platformDunning` → CTA becomes **Pay now → payUrl** when present (else "View your
  billing"), and "Manage billing" is added to the footer links; the "how to pay"
  copy switches (PAY_NOW_INSTRUCTIONS vs PAY_INSTRUCTIONS). Provider-agnostic — the
  same pattern carries Airwallex/Stripe hosted pay URLs later. Preview shows the
  Pay-now variant.

## N4 Slice 2 — win-back ✅ DONE (verified)
`sweepWinBack()` in the lifecycle sweeper: a customer whose MOST RECENT visit was
~60 days ago (window 60–63d, non-cancelled, no later/upcoming booking) →
`sendWinBack(bookingId)` (tenant brand, "We miss you" + "Book your next visit" CTA,
opt-out honored). Re-sendable after a 90-day cooldown (stamped
`metadata.lifecycle.win_back_sent_at`). Per-site gate `metadata.lifecycle_emails.
win_back` (default on). Preview `/dev/preview/win-back`. Verified: lapsed customer
→ sent + stamped; re-sweep sent 0 (cooldown). REMAINING N4: post-appointment
review-ask · birthday · anniversary · no-show · CMS "Emails" page.

## Tenant-email fixes (Peter, 2026-07-13)
- **Business name linked in the footer**: "Sent by {business}" now links {business}
  to the business's own site. `renderEmail` `brand` gained `url`; `baseEmail.js`
  footer links the name when present; all 8 tenant builders take `businessUrl` →
  `brand.url`; `loadBookingContext` exposes `businessUrl` (→ the ...c-spread senders:
  reminder/cancel/reschedule/first-visit/win-back get it free); `bookingController`
  `getTenantEmailBits` returns `businessUrl` for the 3 confirmations.
- **Preview logo bug fixed**: the first-visit/win-back preview routes were passing
  the STEMFRA logo as the business logo — the REAL sends were always correct (they
  use `theme.logo_url`, and Argyle has one). Previews now use Argyle's real logo +
  linked URL. Sent real first-visit + win-back to peter.space.io@gmail.com to verify.
- **Dark corners around tenant logos fixed** (Peter spotted on the Forge & Bell
  confirmation): tenant logos are stored as **transparent WebP** (correct on the
  colored backgrounds of their live site), but Gmail drops WebP alpha and paints the
  transparent corners BLACK (Outlook won't render WebP at all). Root cause confirmed
  by pulling the source: corners are `[0,0,0,0]` (transparent, black RGB). Fix:
  `baseEmail.js emailLogoUrl(url)` rewrites Cloudinary logo delivery URLs to
  `.../upload/b_white,f_png/...` for EMAIL ONLY — flattens onto the white card,
  forces PNG (no alpha to mishandle, universal client support). Stored asset
  untouched. Applied to the tenant header `<img>`; non-Cloudinary URLs pass through.
  Verified in the preview pane — corners now blend into the card.

## N4 slice 3 — review / feedback ask ✅ DONE (verified end-to-end)
Post-visit review ask, ~2 days after a booking. Tenant-branded, opt-out honored,
once ever per customer.
- **`templates/transactionalEmails.js` `reviewRequest(...)`** — optional per-site
  `reviewUrl` (Google/Yelp): present → CTA "Leave a review"; absent → warm
  "reply and tell us how it went" fallback (CTA falls back to "Book your next
  visit"). Two preview routes (`/dev/preview/review-request` +
  `review-request-noreview`).
- **`lib/bookingEmails.js`** — `loadBookingContext` now exposes
  `reviewUrl` (from `site_theme_settings.metadata.review_url`).
- **`lib/lifecycleEmails.js` `sendReviewRequest(bookingId)`** — gate on
  `prefs.review_ask`; **once ever** (`lifecycle.review_ask_sent_at`); **skips if a
  first-visit email went out in the last 3 days** (`REVIEW_ASK_FIRST_VISIT_GAP_DAYS`)
  so a brand-new customer isn't emailed twice in 48h. `getLifecyclePrefs` gained
  `review_ask` (default on).
- **`lib/lifecycleSweeper.js` `sweepReviewAsk()`** — bookings 2–3 days ago,
  confirmed/completed, live site; wired into `sweepOnce()` (order: first-visit →
  review-ask → win-back).
- **Verified**: seeded a completed argyle booking 2 days ago → 1st send `true`,
  2nd `false` (once-ever), first-visit-gap `false`. Test mail to
  peter.space.io+reviewtest@gmail.com; seed cleaned up.
- ⚠️ `review_url` has NO CMS surface yet — comes with the CMS "Emails" page (which
  will also toggle `lifecycle_emails.*`). Until then it degrades to the reply-only
  fallback, which is fine.

## N4 slice 4 — birthday greeting ✅ DONE (verified end-to-end)
Warm birthday wishes on the customer's birthday. **Customer-based, not
booking-based** — first N4 email driven off the customer record (`birthdate`
captured in N2). Once per calendar year per customer.
- **Refactor (shared, low-risk)**: `lib/bookingEmails.js` gained
  **`loadSiteBrand(siteId)`** + **`resolveBusinessEmail(siteId)`** — the site-side
  brand bits (name/logo/url/email/bookingUrl + `reviewUrl` + `birthdayOffer`) with
  NO booking/customer, for customer-based lifecycle mail. `loadBookingContext` now
  reuses `resolveBusinessEmail` (same location_map lookup, deduped). Smoke-tested:
  booking context `businessEmail` still resolves.
- **`templates/transactionalEmails.js` `birthdayGreeting(...)`** — optional
  per-site `birthdayOffer` (free text, e.g. "15% off any service"): present → shown
  as a gift line; absent → generic "treat yourself" line. No promo-code system
  needed. Two previews (`/dev/preview/birthday` + `birthday-nooffer`).
- **`lib/lifecycleEmails.js` `sendBirthday(customerId)`** — gate on `prefs.birthday`;
  **once per year** (`lifecycle.birthday_sent_year`); opt-out honored. Does NOT
  self-gate on site status (the sweeper owns that — made consistent with the other
  four senders; a stray `!== 'live'` check was removed after it wrongly blocked a
  previewing-site test). `getLifecyclePrefs` gained `birthday` (default on).
- **`lib/lifecycleSweeper.js` `sweepBirthday()`** — customers whose birthday
  (month+day) is today, live site; filters MM-DD in JS (PostgREST can't MM-DD-match
  a date column — fine at current volume; noted a generated-column+index path for
  scale). Wired into `sweepOnce()`.
- **Verified**: seeded an argyle customer with birthdate=today → 1st send `true`,
  2nd `false` (once-per-year), stamp `{birthday_sent_year:2026}`. Test mail to
  peter.space.io+bdaytest@gmail.com; seed cleaned up. Preview screenshot reviewed.
- ⚠️ `birthday_offer` (and `review_url`) have NO CMS surface yet — both come with
  the CMS "Emails" page; until then birthday degrades to the generic line.

## N4 slice 5 — first-visit anniversary ✅ DONE (verified end-to-end)
"Happy anniversary" ~1 year after a customer's first visit. Booking-based, mirrors
first-visit; once ever per customer.
- **`templates/transactionalEmails.js` `anniversaryGreeting(...)`** — warm "thanks
  for a year" + rebook CTA. Preview `/dev/preview/anniversary`.
- **`lib/lifecycleEmails.js` `sendAnniversary(bookingId)`** — gate on
  `prefs.anniversary`; **once ever** (`lifecycle.anniversary_sent_at`); opt-out
  honored. `getLifecyclePrefs` gained `anniversary` (default on).
- **`lib/lifecycleSweeper.js` `sweepAnniversary()`** — bookings ~365 days ago that
  were the customer's FIRST visit (same first-visit detection as `sweepFirstVisit`),
  live site. Wired into `sweepOnce()`.
- **Verified**: seeded a first-visit argyle booking 365 days ago → 1st `true`, 2nd
  `false` (once-ever), stamp `anniversary_sent_at` set. Test mail to
  peter.space.io+annivtest@gmail.com; seed cleaned up. Preview screenshot reviewed.

## N4 slice 6 — no-show follow-up ✅ DONE (verified end-to-end)
Warm "sorry we missed you" when the owner marks a booking no-show. **Event-based**
(not swept) — fires from the CMS booking-status change, like owner-cancellation.
- **`templates/transactionalEmails.js` `noShowFollowup(...)`** — no-guilt rebook
  nudge. Preview `/dev/preview/no-show-followup`.
- **`lib/lifecycleEmails.js` `sendNoShow(bookingId)`** — opt-out honored + per-site
  toggle (`prefs.no_show`, default on). Lives in lifecycleEmails (opt-out/prefs
  infra) but is EVENT-triggered, not in the sweeper.
- **`routes/cms/bookings.js` `/notify`** — accepts `event: 'no_show'` → `sendNoShow`
  (tenant-scoped ownership check, same as cancelled/rescheduled).
- **CMS**: `notifyBookingEmail` event union += `no_show`; `useUpdateBooking`
  (queries.ts) fires it when `patch.status === 'no_show'` (fire-and-forget, mirrors
  the cancelled path). `no_show` is owner-settable via `BookingDetailModal`
  (confirmed: free-text status column, no CHECK constraint).
- **Verified**: seeded an argyle no_show booking → `sendNoShow` `true`; CMS
  typecheck clean; preview screenshot reviewed. Test mail to
  peter.space.io+noshowtest@gmail.com; seed cleaned up.

### N4 lifecycle status — COMPLETE
All six lifecycle emails live: **first-visit, review-ask, win-back, birthday,
anniversary** (swept) + **no-show** (event-based). Prefs model
`site_theme_settings.metadata.lifecycle_emails.{first_visit,win_back,review_ask,
birthday,anniversary,no_show}` (opt-out, default on), plus `review_url` +
`birthday_offer` content fields. **Remaining N4 surface**: the **CMS "Emails" page**
to toggle all six + set `review_url` / `birthday_offer` (sweeper + senders already
read them).

## N4 — per-email discounts + CMS "Emails" page ✅ (server + CMS built; live visual walk pending Peter login)

### Lifecycle discounts (Peter, 2026-07-13)
Owners can attach an optional **% discount** to any of the positive-milestone
emails, configurable, default 15%.
- **`templates/baseEmail.js` `discountBlock(text)`** — a warm brand-neutral
  "coupon" band (amber fill + dashed hairline), exported.
- **`templates/transactionalEmails.js`** — `discountLine(percent, lead)` helper +
  a `discountPercent` param on **first-visit, review-ask, win-back, birthday,
  anniversary** (NOT no-show — wouldn't reward no-shows). `> 0` → renders the
  coupon; `0`/absent → nothing. **`birthday_offer` free-text field REMOVED** →
  replaced by the structured percent.
- **Data model**: `site_theme_settings.metadata.lifecycle_offers[key] =
  {enabled, percent}` (default enabled:false, percent:15 — owner opts in).
  `loadBookingContext` + `loadSiteBrand` expose `lifecycleOffers`;
  `lib/lifecycleEmails.js` `offerPercent(offers, key)` (enabled + 1–100) resolves
  it and each sender passes `discountPercent`. Preview `/dev/preview/birthday` now
  shows the 15% coupon; `birthday-nooffer` shows none.

### CMS "Emails" settings page
- **`components/settings/EmailsSection.tsx`** (Settings → Notifications group, id
  `lifecycle-emails`) — one row per lifecycle email with an on/off toggle
  (`metadata.lifecycle_emails`), and for the 5 discount-capable ones an "Include a
  discount" toggle + a % input (`metadata.lifecycle_offers`), plus a **Review link**
  field on review-ask (`metadata.review_url`). Discount/review controls only show
  when the email is on. Mirrors `NotificationsSection` (Switch + draft/dirty/save via
  `useUpsertThemeSettings`). Registered in `settingsSections.ts` + `SettingsPage` +
  `sectionIcons`.
- **Verified**: CMS typecheck clean; **CMS→server contract proven end-to-end** —
  wrote the exact shape `EmailsSection.save()` produces onto argyle's theme
  (`win_back:false` + `birthday {enabled:true,percent:20}` + review_url), confirmed
  the server reads it (`getLifecyclePrefs` suppresses win-back; `loadSiteBrand`
  resolves the 20% + link), then restored the metadata cleanly. CMS dev server
  (:5180) serves the page. **⚠ Live authenticated visual walk of the page is
  pending — Claude cannot type the login password (safety rule); Peter to sign in
  (peechizzy or staff @stemfra.com), then Claude drives the authenticated session.**

## N5 tail — prod env + branded Supabase auth emails ✅ (Peter dashboard actions pending)
- **`deploy.yml`** — added `EMAIL_PROVIDER=resend` + `RESEND_API_KEY` (secret) +
  `RESEND_FROM_DOMAIN` + `RESEND_FROM_ADDRESS` to the env block so prod sends via
  Resend. Ready for push (holding commits per Peter's rule).
- **Branded Supabase auth emails** (the "Forgot login" gap, CMS owners + members):
  `templates/authEmails.js` renders **confirm-signup / magic-link / reset-password /
  change-email** through the Case 9 base (Stemfra brand). `{{ .ConfirmationURL }}`
  passes through `escapeHtml` untouched → lands raw in the button href (verified).
  Preview routes `/dev/preview/auth-*`; paste-ready HTML exported to
  `docs/supabase-auth-templates/*.html`; runbook `docs/SUPABASE_AUTH_EMAILS.md`.
  Reset-password preview screenshot reviewed (logo, centered heading, violet CTA,
  security line, tidy footer).
- **⚠ Peter dashboard actions** (can't be done from code): (1) Supabase → Auth →
  Emails → enable custom SMTP (smtp.resend.com, user `resend`, pass = RESEND key,
  sender `notifications@mail.stemfra.com`); (2) paste the 4 templates + subjects.
- **N5 remaining (flagged, not built)**: security track (login alerts, 2FA recovery
  codes) — a real build; send-from-their-domain Pro perk — per-tenant Resend domain
  verification; marketing help-center docs — content; tighten root DMARC
  `p=none`→quarantine — Peter reviews CF DMARC reports first.

## NEXT (Peter to choose)
- **N2** — owner notification controls + customer prefs + **team-member email
  field + "copy on their bookings"** + System A invoice/dunning emails
  (provider-agnostic) + resend-confirmation/schedule tools. (Recommended next.)
- Remaining N5 tail (later): Supabase auth emails on the base + custom SMTP;
  security track (login alerts, 2FA recovery); send-from-their-domain Pro perk;
  help-center docs; tighten root DMARC once reports look clean; add Resend env to
  deploy.yml for prod.
