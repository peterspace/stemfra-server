# Work log — 2026-07-07 · CMS Beta Test (peechizzy account)

**Mission**: operate the CMS like a user (Playwright-driven — preview_* MCP tools disconnected this session), find + fix bugs across all 16 demo sites / 6 verticals. Test account: peechizzy@gmail.com. Out of scope: buy-domain (Porkbun unfunded), real Stripe charges, git pushes.

**Test fleet** (16 sites, all owned by peechizzy):

| Vertical | Site (subdomain) | Theme | Status at start |
|---|---|---|---|
| barbershops | argyle-and-sons | barbershops-classic-nyc | previewing |
| barbershops | rourke-sloane | barbershops-manhattan | live |
| crossfit | 212-strength-co | crossfit-212 | live |
| crossfit | blackfly-barbell | crossfit-blackfly | live |
| crossfit | ironclad-athletics | crossfit-box | live |
| crossfit | forge-and-bell | crossfit-volt | live |
| massage | lull | massage-escape | previewing |
| massage | reveline | massage-reverie | previewing |
| massage | umbra | massage-zenhaven | previewing |
| salons | vesper-beauty-house | salons-beauty-house | live |
| salons | maison-lune | salons-sorrel | live |
| spa | ellaris-spa | spa-ellaris | previewing |
| spa | vela | spa-lumora | previewing |
| spa | aurea | spa-respira | previewing |
| yoga_pilates | wildflower-yoga-pilates | yoga-pilates-sanctuary | live |
| yoga_pilates | lila-studio | yoga-pilates-sawiet | live |

## Pre-test fixes (carried into this session)

- **FIX**: Publish-checklist links were dead — `siteCompleteness.js` routes predated the settings split (`/settings` → redirects back to `/settings/publish`; `/content` → index). Repointed: logo → `/settings/style#branding`, hours → `/settings/hours#hours`, social → `/settings/social#social`, address/phone/headline/personalize → `/content/home`, business name → `/profile`. *Verify in Phase 1.*
- **GAP (flagged, not fixed)**: no self-serve business-name editor in the CMS (`companies.name` set at provisioning only).

## Test log

(chronological; ✅ pass · 🐛 bug found · 🔧 fix applied · ⏭ deferred)

### Phase 0 — setup
- Preview MCP tools disconnected this session → testing driven by **Playwright** (headless Chromium from stemfra_server's install). Harness: `scratchpad/beta/{lib,00_login,01_sweep,02_checklist_links}.js`; auth via storageState.
- ✅ Login as peechizzy@gmail.com works; `/sites` renders all **16** demo sites.
- Started template dev servers 5174–5177 (were down); 5181/5182/5180/4000/5178 already up.
- Note: `net::ERR_ABORTED` Supabase requests during navigation are React Query cancellations — filtered as noise, not bugs.

### Phase 1 — smoke sweep + checklist links
- ✅ **Publish-checklist logo link fix VERIFIED** (reveline): "Upload your logo in Settings." → `/settings/style#branding`, anchor exists, page renders. (Only the logo item renders a link on reveline — the other items are ✓.)
- ✅ **Full sweep: 16/16 sites CLEAN** — 26 routes each (dashboard, content, services, team, testimonials, blog, media, leads, bookings, schedule, memberships, all 7 settings groups + publish, billing ×3, notifications, profile ×2) = ~416 page loads. Zero console errors, zero pageerrors, zero 4xx/5xx, zero blank pages.
- One transient `Failed to fetch` on reveline dashboard during the 4-parallel-browser run; clean on solo re-run — load artifact, not a bug.

### Phase 2 — interactive tests (1 site per vertical)
- ✅ **Hero headline edit → save → reload-persist → template render → restore**, full pass on **barbers** (argyle/:5174), **salons** (maison-lune/:5175), **crossfit** (forge/:5176), **massage** (lull/:5181), **spa** (ellaris/:5182).
- 🟡 **yoga** (lila-studio/sawiet): save + persist ✅, but the template never shows the headline — `hero: wordmark-overlay` renders the BRAND as hero text by design (verified: "A space to reconnect" on the page is the intro section from DB, not a fallback). **Not a data bug.** **UX gap flagged**: the hero editor offers Headline/Subheadline with no notice that the active theme's hero ignores them (owner would think saving is broken — exactly how it read in this test). Candidate fix: theme-aware field notice like ServiceGrid's "this theme doesn't show these cards" — needs a variant→used-fields map; discuss before building (hardcoded variant lists in the CMS = the staleness pattern we just cleaned).
- 🐛→🔧 **Default-theme catalog drift** (found via theme-switch prep): barbershops' `is_default` template was **Classic Argyle (inactive)** — deactivated in walk correction 44 without moving the default; salons had **no default at all**. Not a provisioning breakage (`resolveTemplate` filters `is_active` and falls back to lowest display_order active), but the catalog lied and the CRM Templates page flags both states. **Fixed via SQL to match effective behavior**: barbershops default → Classic NYC, salons default → Sorrel. (Change in CRM → Templates if a different default is wanted.)
- ✅ **Theme switch round-trip** (argyle): Classic NYC → Manhattan → Classic NYC via the picker + confirm dialog; active-card state correct each step.
- ✅ **Social handles** (forge): TikTok set → saved → persisted after reload → restored.
- ✅ **Opening hours** (maison-lune): Monday close 17:00→18:30 → saved → persisted → restored.
- ✅ **Prices toggle** (ellaris): Hide → `site_theme_settings.metadata.show_prices:false` in DB; Show → cleared to null. Exact expected writes.
- ✅ **Logo upload** (reveline): media-picker Upload tab → PNG auto-WebP'd to Cloudinary `reveline/` folder → `logo_url` saved → publish checklist's Recommended block now hides (all items ✓ — correct render logic). **Left the placeholder "R" monogram logo in place** so reveline's checklist is complete for publishing; replace at will.
- ✅ **Blog round-trip** (ellaris): create → auto-slug → publish → renders on template `/blog` index AND `/blog/:slug` article → deleted from CMS (row Delete + confirm). (First delete attempt failed on MY test selector, not the CMS — verified clean on retry.)

### Phase 3 — full lifecycle (create → publish → delete)
- 🐛→🔧 **Stripe-checkout return URLs dropped the billing param**: `publishController.billingCheckout` sent Stripe back to `/settings?billing=success`, but `/settings` redirects via a plain-string `<Navigate>` which **drops query strings** — so after paying, the owner never got the success toast / readiness poll / auto-publish (recoverable manually, but the paid moment silently broke). **Fixed both sides**: server URLs → `/settings/publish?billing=…`; CMS `/settings` redirect → `SettingsRedirect` component preserving search+hash. Verified: `/settings?billing=cancelled` now lands on `/settings/publish` and fires the "Checkout cancelled." toast.
- 🐛→🔧 **CRITICAL: owner "+ New site" (and owner clone) broken for real clients** — `POST /api/cms/sites` failed with FK violation: `sitesController` passed `createdBy: req.cmsUser.id` into `sites.created_by`, whose FK → `profiles(id)` (STAFF-only since Wave 0). The fixture owners have legacy profile rows so it always worked in testing; real clients (peechizzy) have none → 500 on every new-site/clone attempt. The signup path (`onboardSite.js`) already documented the correct convention (no createdBy for client actors). **Fixed**: dropped `createdBy` from both `create()` and `cloneOwnSite()` (owner captured via `owner_contact_id`). Verified: created beta-test-barbers + beta-test-spa via the UI as peechizzy.
- ✅ **Provisioning**: both clones fully seeded (9 pages, services, team) and landed on the just-fixed default themes (Classic NYC / Ellaris) — spa provisions correctly post-un-deferral.
- ✅ **Publish**: seeded active test subscriptions (SQL, `metadata.beta_test`) → checklist all-required-✓ (and the personalize/demo-content detection correctly flagged the seeded hero/address) → "Publish — Go live" → both `status=live`, **real CF CNAMEs attached, live URLs 200 via the Cloudflare edge, and each host renders its own tenant** (multi-tenant-by-host proven for barbers AND spa on prod infra).
- ✅ **Delete**: CMS Sites-page delete (type-DELETE confirm) → `deleted_at` stamped, subscriptions → cancelled, CNAMEs detached (hosts now 530). Hard-purged both via `hardPurgeSite` → zero orphans (sites/companies/subs/pages/services all 0); `companyDeleted:true` both times (this session's purge extension works).
- 🐛 **FLAGGED (needs Peter's decision — RLS/schema)**: a **soft-deleted site stays anon-readable for the full 90-day grace window** — status stays `live` and the anon RLS policies don't check `deleted_at`, so all site data remains queryable via the public REST API (verified live against beta-test-barbers post-delete). CNAME detach hides it from browsers only. Options: (a) add `deleted_at is null` to the anon `sites` policy (+ dependent child policies), or (b) softDelete parks `status` in a non-public value and restore reads the prior status from metadata. Recommend (a).

### Phase 4 — cross-verification (Supabase + CRM)
- ✅ **Notification triggers**: all 8 `trg_notif_*` triggers present + enabled; live-fired a lead insert on reveline → `lead_created` (operations, "New lead") row created; test lead + notification cleaned up. (The lifecycle sites' notification/audit rows were cascade-purged with the sites — by design.)
- 🐛→🔧 **CRM dev sign-in hung on "Signing in…" forever** — reproduced WITHOUT touching Google, which disproves last session's hung-OAuth diagnosis. Real cause: `Login.jsx handleDevLogin` never navigates after a SUCCESSFUL `signInWithPassword` (session verified in localStorage; manual goto `/` landed authed on /dashboard). Google only works because OAuth full-page-redirects. **Fixed**: `window.location.replace('/')` on success (mirrors the OAuth boot path). Verified: dev login now lands on the dashboard. Memory `crm_dev_login` updated.
- ✅ **CRM Customer Sites**: 16 tenant hosts listed, zero beta-test leftovers, massage + spa verticals present, no page errors.

## End-of-session state
- **Demo sites**: all 16 restored to pre-test state (hero/hours/social/theme/prices) EXCEPT reveline now has a placeholder "R" logo (deliberate — completes its publish checklist; replace at will).
- **Test artifacts**: beta-test-barbers + beta-test-spa fully purged (DB + CF); seeded test subscriptions purged; test lead/notification/blog post deleted.
- **Code changes (LOCAL, not pushed)**: `stemfra_server/lib/siteCompleteness.js` (checklist routes), `stemfra_server/controllers/cms/publishController.js` (checkout return URLs), `stemfra_server/controllers/cms/sitesController.js` (created_by FK fix ×2), `stemfra_cms/src/App.tsx` (SettingsRedirect preserves query), `stemfra-ops/src/pages/Login.jsx` (dev-login redirect). DB-only fixes: default-theme flags (barbershops→Classic NYC, salons→Sorrel).
- **Open items for Peter**: (1) soft-delete anon-readability RLS decision; (2) wordmark-hero editor UX notice; (3) no self-serve business-name editor; (4) buy-domain untested (Porkbun unfunded, per instruction).

## Flagged-item fixes (2026-07-08 — Peter: "fix all the flagged items + Stacy sync")

- ✅ **Soft-delete anon RLS closed** (open item 1). Added `deleted_at is null` to the anon `sites_public_read_live` policy. Child tables (services/sections/theme/pages/posts/companies) all gate through a `sites` sub-select or `company→live-site` EXISTS, so they close automatically. **Verified with a throwaway clone**: created rls-probe-site → anon read OK while previewing → soft-deleted → anon reads of sites/services/sections/theme/company ALL return `[]`, while a live demo site stays readable → hard-purged (zero residue). Restore still works (status/deleted_at cleared).
- ✅ **Theme-switch stale-override clear** (open item / "pay attention to stale state"). `useSwitchTemplate` now also nulls the theme-scoped `site_theme_settings` columns (primary/accent/background/text colors + display/body fonts) AND strips presentation metadata keys (hero_variant, header_variant, footer_style, color_extras, nav_mode) so the new theme renders from ITS defaults — kills the recurring "wrong fonts/colors after switching" trap at its source. Content-ish settings (logo, socials, announcement, show_prices, newsletter, onboarding) deliberately survive. Confirm-dialog + subtitle copy updated. **Verified**: planted a fake stale override on argyle (red accent, Comic Sans, hero_variant, color_extras) → switched NYC→Manhattan→NYC via the picker → all overrides null, logo/socials preserved.
- ✅ **Wordmark-hero editor notice** (open item 2). New `HERO_FIELD_USAGE` map in `@stemfra/archetypes` (Hero/fieldUsage.ts) — a `satisfies Record<HeroVariant,…>` so adding a hero variant without an entry is a COMPILE error (staleness-proof). New `useHeroVariant(siteId)` hook (per-site override → template default) + a `ThemeFieldNote` under Eyebrow/Headline/Subheadline in HeroSectionEditor. **Verified**: lila-studio (wordmark-overlay) shows "your theme shows your business name as the main text…" under Headline; argyle (cinematic-right) shows no note. Reacts to theme switches (keyed under theme-settings).
- ✅ **Self-serve business-name editor** (open item 3). New `PATCH /api/cms/sites/:siteId/business-name` (updates `companies.name`, ownership-checked, audit-logged) + `useUpdateBusinessName` hook + `BusinessNameSection` (Settings → Style, top). Publish-checklist `business_name` route now points at `/settings/style#business` (was /profile). **Verified**: renamed forge via the UI → DB updated → TopBar/chrome reflected it → restored; checklist anchor resolves + editor renders.
- ✅ **Stacy sync — the real fix** (Peter's Q "how can Stacy stay in sync automatically?"). Two knowledge surfaces:
  - **Routes**: found Stacy's onboarding had the SAME stale `/settings` routes I'd fixed in the publish checklist (logo/hours/social → redirect loop). Root cause: the checklist and Stacy each hardcoded CMS paths independently → they drift. **Fix = single source of truth**: new `lib/cmsRoutes.js` (`CMS_ROUTES` map + `contentRoute()`), imported by BOTH `siteCompleteness.js` (checklist) AND `stacyOnboarding.js`. A route rename is now one edit → both stay in sync forever. Verified: Stacy onboarding returns zero bare `/settings`; live-tested logo→/settings/style#branding + hours→/settings/hours#hours navigate + scroll through the actual Stacy setup UI. Also fixed the CMS `StacyPanel` `ROUTE_LABEL` map (stale `/settings` key) + a prefix fallback so guidance text never shows a raw path.
  - **Data context**: `buildSiteContext` live-reads the DB, so content edits already auto-sync — but it was BLIND to blog + active theme. Added `business.theme` (template display_name) + `blog{total,published,drafts}` (site_posts). Verified: umbra shows theme "Umbra" + 3 published posts; calm-roots shows "Escape" + 0.
- **New local changes this turn (not pushed)**: `stemfra_server/lib/{cmsRoutes.js NEW, siteCompleteness.js, stacyOnboarding.js, stacyContext.js, controllers/cms/sitesController.js, routes/cms/sites.js}`; `stemfra_cms/src/{lib/queries.ts, lib/useSites.ts, lib/settingsSections.ts, pages/SettingsPage.tsx, components/settings/{BusinessNameSection.tsx NEW, ThemesSection.tsx, sectionIcons.tsx}, components/sections/HeroSectionEditor.tsx, components/StacyPanel.tsx}`; `packages/archetypes/src/Hero/{fieldUsage.ts NEW, index.tsx}`; CMS package.json (+@stemfra/archetypes dep). DB: anon sites RLS policy; barbershops/salons default-theme flags (from 07-07). All typecheck green.
- **Note**: found `:4000` server was down mid-turn (it had been up all session) — restarted it via `npm --prefix … run dev` in background. Peter usually runs it himself; if he has his own terminal instance, the background one is redundant but harmless.
- 🐛→🔧 **Starters ignored soft-delete** (Peter's "if anyone is deleted" question): `listStarters` filtered by status only — soft-delete keeps `status='live'`, so a deleted demo stayed in the catalog AND `getApprovedStarter` would still let signups CLONE it (service-role bypasses the anon RLS fix). Fixed both: `.is('deleted_at', null)` on the list + a `deleted_at` guard in the clone whitelist. Verified: 16 live demos still returned.
- 🐛→🔧 **Marketing site pointed at 3 PHANTOM demo subdomains** (`stemfra_client`): `themes.js` + `WorkMarquee.jsx` referenced `halsey-crowe`, `linden-lark`, `voltage-strength` — subdomains that never shipped (written against a superseded provisioning plan). The Themes pages' "View demo" links for Classic NYC / Sorrel / Volt were live-broken (530), and the marquee's saved-mockup upgrade could never match those keys. **Fixed**: repointed to the real fleet (argyle-and-sons / Argyle & Sons, maison-lune / Maison Solène, forge-and-bell / Forge & Bell), cities corrected from each demo's own location content, Wren Yoga brand fixed, `verticals.js` "Yoga & Pilates"→"Yoga". Sync comments now name `GET /api/starters` as the live truth. Verified in preview: /themes/barbers + /themes/crossfit render correct brands + real demo links; home marquee clean, zero console errors. `demoLinks.js` (Mark's outreach) checked — all 4 flagship links are REAL live sites, no action needed.
- 📋 **Mockups → marketing site**: pipeline already exists end-to-end (CRM save → `metadata.marketing_mockups` → public `GET /api/marketing/mockups` → WorkMarquee prefers saved over fallback) but the inventory is EMPTY (0 saved mockups on any demo — likely lost with the deleted duplicates in the consolidation). Placement options presented to Peter for discussion.
## Mockups → marketing site arc (2026-07-08, LOCAL — nothing committed)

**Decisions locked with Peter**: one showcase theme per vertical — Manhattan (rourke-sloane) · Beauty House (vesper-beauty-house) · Volt (forge-and-bell) · Sanctuary (wildflower-yoga-pilates) · Zen Haven (umbra) · Ellaris (ellaris-spa). Scene→surface mapping: Grid (angled) → home WorkMarquee · Tablet → Products drawer preview · hero-fold → theme cards (Peter's call mid-arc, replacing the earlier page-panels-on-cards choice — panels are unreadable at card size and mShots showed "Generating Preview…") · Desktop+Mobile (hero) + Page panels (story) → the NEW theme showcase page.

- **Headless mockup generator** (`scratchpad/mockupgen/{generate,hero_folds}.js`, resumable via state.json): staff login → `screenshot-demo` fold captures + full-page masters (desktop 1280 + mobile 430 → `masterUrl`) → `capture` composites → `save` with the CRM's exact save shape. **39 mockups produced**: 6 demos × 4 scenes + 15 hero-folds (every theme card in the catalog). 🔧 Fixed a real server bug en route: `screenshotDemo`'s `isMobile` evaluated `undefined` on the fold path — Playwright hard-rejects non-boolean (`Boolean()` wrap in mockupsController).
- **Wellness demo hosts had NO DNS** — the rename to lull/umbra/reveline/vela/aurea happened after CNAME attach under the OLD names, and `ellaris-spa`'s spa-project attach never ran. `attachSiteDomain` ×6 → all live (delays were local negative-DNS caching; CF validated active).
- **Server**: `marketingMockups` now returns the newest mockup per **(subdomain, scene)** + skips soft-deleted demos — each marketing surface consumes its own scene; re-saving in the CRM re-skins only the matching surface.
- **stemfra_client**: new `lib/mockups.js` (`useMockupMap(scene)` module-cached fetch + `SHOWCASE_BY_VERTICAL`); **WorkMarquee** rebuilt → 6 items (one per vertical, grid-angled scene, null-fallback items skipped until saved; conveyor isolated in a keyed child so item-count changes re-mount cleanly); **verticals.js** += `wellness` group + massage/spa verticals (all consumers iterate dynamically — home grid, Footer, Examples, Start, Contact pick them up); **BrowseDrawer** += wellness group order + PreviewPane serves the live TABLET mockup per vertical; **themes.js** += massage (Zen Haven/Escape/Reverie) + spa (Ellaris/Lumora/Respira) theme entries with real brands/cities; **ThemeGallery** cards → live hero-fold captures (mShots relegated to fallback) + card/`Preview theme` → the new **`/themes/:vertical/:theme` ThemeShowcase** page (Desktop+Mobile hero, Page-panels story, Start-with-this-design + View-live-demo CTAs).
- **Verified in preview**: marquee = 12 slides all CRM composites; drawer = 3 groups/6 verticals + CRM tablet preview; /themes/crossfit = 4 CRM fold cards (the "Generating Preview…" mShots embarrassment gone); /themes/crossfit/volt showcase renders both mockups + CTAs; /themes/massage = 3 CRM cards.
- **Open follow-ups**: un-flag yoga's `comingSoon` (its hero-fold now exists)? · Examples.jsx (/templates) cards still use static screenshotPath · wellness pricing/tiers exposure on Pricing page unconfirmed · production `VITE_API_URL` must point at api.stemfra.com for the live maps.

### Showcase marquee + CRM auto-load (2026-07-08 later, LOCAL)
- **Theme-card polish (Peter)**: theme-name badge removed from the card image → now a tracked-uppercase line under the brand ("Rourke & Sloane / MANHATTAN / Barbershop · New York"). Applied to live + ComingSoon cards.
- **ThemeShowcase rewritten → ONE arrow marquee** (Peter's spec): 3 slides — Page panels ("Page tour") · Desktop + Mobile ("Desktop & mobile") · Board 9 angled ("The full picture") — prev/next arrows + dots + caption, slide-fade via AnimatePresence; slides appear per-theme as their scenes get saved (falls back to a single live screenshot when none); the separate "Inside the theme" section is gone; **CTAs moved under the marquee**. Transparent backgrounds DROPPED per Peter mid-arc (backdrops stay; tunable later in the CRM composer — NB my earlier "transparency broken" reading was wrong: Cloudinary `b_` doesn't flatten PNG alpha, the dark was the viewer canvas; rourke's transparent set is real and kept). Verified via Playwright (preview MCP disconnected): 3 slides, arrows advance ("Page tour"→"Desktop & mobile"), CTAs below, zero page errors.
- **🐛→🔧 CRM Mockups auto-load** (Peter: "picking a demo always shows the default — you can't tell what was saved"): saved mockups WERE persisted (sites.metadata.marketing_mockups — the "Saved (n)" button proves it) but the composer never loaded them on selection. `MarketingMockups.jsx` demo-switch effect now auto-loads the NEWEST saved mockup (current scene preferred, else newest overall; hero-fold/non-composer scenes excluded; draft-restore path untouched). Verified: selecting Rourke & Sloane → "Loaded" toast, Saved (9), composer slots populated from stemfra_assets.
- **v3 generation batch running**: board-9-angled for the remaining 14 demos + page-duo/desktop-mobile for the 9 non-showcase demos (default backdrops) — the marquee fills in per theme automatically as saves land.
- **Fixed a real server bug** (from the transparent experiments): `mockupsController.screenshotDemo` isMobile non-boolean on the fold path — `Boolean()` wrap (was breaking ALL fold captures).
- **v3 batch COMPLETE**: 72 live mockup entries across all 15 theme demos — every theme has the full marquee set (page-duo, desktop+mobile, board-9) + hero-fold; showcase demos also carry grid + tablet. Non-showcase marquee verified (Escape/lull: 3 slides, zero errors).
- **Front Desk enabled on all 16 demos (Peter's ask)**: `metadata.frontdesk_enabled=true` on every starter's theme row. Pre-checked: widget mounted in ALL 6 template Layouts (massage/spa inherited it via the yoga clone) + `FRONTDESK_N8N_URL` configured. Verified: launcher renders on rourke/ellaris/umbra live; full chain round-trip on rourke ("What are your opening hours?" → grounded reply with its real hours, conversation persisted). NB: demo visitors can now create REAL leads + (free-service) bookings on the demo sites via chat — expected demo noise; owners can flip the toggle per-site at Settings → Front desk.
- **Demo logos generated + assigned (Peter's ask)**: `scratchpad/mockupgen/gen_logos.js` — theme-aware rounded-square monograms (accent-color tile, luminance-contrasted initials, serif/sans matched to the display font; "X & Y" brands → 2 letters, digit brands keep the number) → Cloudinary `{subdomain}/logo-mark.webp` (CMS folder convention) → `site_media` row → `site_theme_settings.logo_url`. All 15 logo-less demos covered (reveline kept its beta-test mark). Verified live: rourke-sloane (light) + umbra (dark) headers render the marks at nav height. Side effects: publish-checklist "Logo" item now ✓ everywhere; boot-loader rings show the mark; owners can replace via Settings → Style. NB: existing hero-fold mockups were captured pre-logo — a future re-capture pass would show logos in the navs.

- 🐛→🔧 **Mockups demo list showed stale brand names** (Peter's spot-check vs the peechizzy fleet): 9 of 16 dropdown entries were pre-rename snapshots — "Lila Studio" ×7 (the wellness clones inherited it), "Maison Lune", "Wildflower Yoga + Pilates". Root cause: `lib/starters.js` labeled starters from **`metadata.starter_label`**, frozen at flag/clone time — the rename pass updated `companies.name` but not these snapshots (the same stale-snapshot pattern as the theme-override trap). **Fix (staleness-proof)**: `listStarters` label now = `starter_label (optional curation override) || LIVE company.name || subdomain` (added the companies join), AND stripped the stale `starter_label` from all 16 demos so live names flow. A future brand rename now propagates automatically to the Mockups demo list, marketing theme cards, and the onboarding Starter catalog (all consume the same `listStarters`). **Verified**: `GET /api/starters` + the CRM Mockups dropdown both show the 16 correct names (Lull/Umbra/Reveline Massage, Ellaris/Vela/Aurea Spa, Maison Solène, Lira/Wren Yoga, …), zero duplicates.


### Pricing restyle + all-themes gallery + marketing_assets system (2026-07-09, LOCAL)
- **Pricing verification (Peter's docs-first gate)**: page content **matches the docs by construction** — Pricing.jsx renders from `GET /api/plans` (the `crm_settings.billing_plans` DB catalog, mirrored to V3 per TIER_VERSIONS.md 2026-06-30), live-features-only per OFFER_TIERS.md; `verticals.js TIERS` is fallback only. TIER_VERSIONS.md IS the versioned offers doc Peter remembered. → **restyle only, zero content changes**.
- **Pricing restyle (Hostinger mechanics, agreed 2026-07-08)**: per-card **"Included free" box** (catalog features matched by /\bfree\b/i — currently the free-custom-domain line — pulled out of the list, gold-tinted box on all 3 cards); **total-cost transparency line** under every price ("+ $1,000 one-time setup · or 12 months for $990/…"); higher tiers now lead with **"Everything in {prev}, plus"** + own additions, inherited list behind a "See everything in {prev}" toggle (replaces the Squarespace full-repeat); featured (Growth) card gains a shadow lift; **CTA subline "No card needed — pay when you publish."** under each Start-for-free (page footnote removed). Verified monthly + annual + toggles in preview, zero console errors.
- **/templates → all-themes gallery (Peter's spec)**: `ThemeGallery.jsx` rewritten to serve BOTH routes — `/templates` = all 15 themes + search (name/brand/city/vertical) + quick-select pills with counts (All 15 · Barbershop 2 · Beauty Salon 2 · CrossFit 4 · Yoga 1 · Massage 3 · Spa 3, `?vertical=` canonical); `/themes/:vertical` = same page pre-filtered (alias — drawer/back-links unbroken; bogus slug keeps its dead-end). `themes.js` += `getAllThemes()`. Old vertical-tiles `Examples.jsx` archived to `_archive/`. Verified: pills filter + URL sync, search composes with filter ("austin" ∩ Barbershop → helpful empty state; ∩ All → the 3 Austin demos), alias intact.
- **marketing_assets system (task: every marketing image event-driven, CRM-managed)**:
  - **Migration**: all 12 hardcoded Unsplash images (home hero + closing CTA, 6 vertical photos, About ×2, Start, Contact) uploaded to Cloudinary `stemfra_assets/marketing/<slot-dashed>` + seeded as slot rows (`page.section.item` dot-paths) via scratchpad `marketing_assets_seed.js`.
  - **Server**: `controllers/admin/marketingAssetsController.js` + `routes/admin/marketingAssets.js` (staff PLATFORM_OPS: GET list · POST /upload multipart {slot,label?,group_key?,alt?}+image → fixed public_id overwrite+invalidate → upsert · PATCH alt/label · POST /delete folder-guarded); public **`GET /api/marketing/assets`** added to routes/marketing.js.
  - **Client**: new `lib/marketingAssets.js` (`useMarketingAsset(slot)` / `useMarketingAssets()`, module-cached like mockups.js). Hero, HomeClosingCTA, BusinessTypes tiles, Pricing DesignPreview, BrowseDrawer preview-pane fallback, About ×2, Start, Contact all read slots; `photoUrl` DELETED from verticals.js. **NO fallback slots per Peter** — missing slot = surface hides (ink base + gradients hold layouts). All slot URLs go through `cldWidth` (f_auto/q_auto + width caps — faster than the old Unsplash hotlinks). Verified: home/about/contact/pricing render 0 unsplash imgs, 12 slot imgs, none broken.
  - **CRM**: sidebar entry → **"Mockups & imagery"**; both pages share a `MarketingTabs` bar — **Mockup studio** (`/marketing-mockups`, h1 renamed) | **Site imagery** (`/marketing-imagery`, new `MarketingImagery.jsx`): slot cards grouped by page (thumb, slot code, dims, inline alt-text save-on-blur, Replace image = file upload to the same slot, Remove with no-fallback warning). AppLayout area map += marketing-imagery→marketing_mockups.
  - **Smoke-tested end-to-end** on a throwaway `test.smoke.photo` slot: upload → public read → PATCH alt → delete → gone (12 real slots untouched).
- **Boundary note**: demo MOCKUPS stay on `sites.metadata.marketing_mockups` (Mockup studio tab); `marketing_assets` is ONLY standalone marketing-site photography.

### Cross-site image reuse (CMS media picker) + ?site= backport + template-route consolidation (2026-07-09, LOCAL)
- **?site=<subdomain> dev param backported** to the 4 older template apps (barbers/salons/crossfit/yoga `siteResolution.ts` — mirrors massage/spa): `localhost:5176/?site=blackfly-barbell` previews any demo on that port without .env edits/restarts; DEV-only, prod host resolution unchanged. Verified live on crossfit. (Context: Peter expected the CMS site-switcher to re-point the template dev port — explained the gap: template resolves via env/host, CMS selection lives in :5180 localStorage, cross-origin invisible; the ?site= param is the dev-ergonomics fix.)
- **Marketing /themes/* → /templates/*** (Peter): ThemeGallery serves `/templates` + `/templates/:vertical`; showcase at `/templates/:vertical/:theme`; pills are path links; legacy `/themes/*` = react-router `redirect` loaders (params preserved). ⚠️ routes.js is plain .js — NO JSX (a Navigate-component draft broke the app; `redirect` loaders are the JSX-free way). Also per Peter: gallery search bar + pill counts removed; pills restyled to the Pricing underline-tab register; hero subheading removed; "Preview theme"→"Preview Website"; pricing cards dropped the per-card setup line (annual keeps "2 months free · $X/mo"; setup stays in the hero only); home "How it works"/"What you'll receive"/"Designed and engineered by people" sections got the standard-name-as-heading fix (eyebrow-in-heading rule from the platform templates).
- **Cross-site image reuse v1 (Peter's feature: reuse assets across owned sites, per-field, images only — clone-at-the-single-entry level)**:
  - **Server**: `POST /api/cms/site-uploads/copy` {sourceMediaId, targetSiteId} — own-BOTH-sites check → Cloudinary duplicates into the target's subdomain folder (pulls the existing URL server-to-server; no second WebP transcode) → new `site_media` row. **Copy, NOT reference** (folder convention + "In use/Unused" scan stay accurate; deleting the original can't break other sites). Images only; same-site pick = no-op returning the original.
  - **CMS**: `MediaSourceModal` Library tab gained a **site picker** (owner's sites from `useOwnerContext`, live company-name labels, "(this site)" marker; images only, hidden for videos/single-site owners) + copy-on-select with a "Copying…" tile overlay and the hint "Picking an image copies it into this site's library". Every image field (hero/gallery/services/team/testimonials/branding) inherits it via the shared modal. `copySiteMedia()` in `lib/useSiteMedia.ts`.
  - **Verified end-to-end**: headless API (magic-link session for peechizzy — the only multi-site owner; copy → `blackfly-barbell/…` key + row + CDN 200; same-site no-op; not-owned target 403; delete-cleanup) + full UI walk in the CMS preview (session injected into :5180 localStorage — 5180 isn't in the Supabase auth redirect allow-list, magic links bounce to :5176; localStorage injection under key `sb-<ref>-auth-token` works): picker lists all 16 sites, Forge & Bell grid loads, pick → ~4s copy → hero field holds an `argyle-and-sons/…` URL. All test copies deleted; hero draft unsaved.
  - **Deliberately NOT built** (Peter): text copy across sites (long-run consistency issues); whole-section copy + theme copy remain future candidates.

### Image-reuse polish: dimension cap + styled pickers + per-section "Use" (2026-07-09 later, LOCAL)
- **WebP audit (Peter suspected CMS uploads skip the transcode)**: FALSE alarm on format — 112/118 stored images ARE WebP (the Media library shows the ORIGINAL upload filename `.jpg`, which misleads; flagged as a UI polish candidate). The REAL storage issue: no dimension cap — a 4480×6720 original stays that size post-WebP (9.7MB). **Fix**: CMS upload now stores images fit to a **2560×2560 `crop:'limit'` box** (`MAX_IMAGE_DIMENSION` in uploadController); the copy endpoint **normalizes legacy sources** (oversized or non-WebP → capped WebP) instead of propagating bloat — verified: a 5890px/990KB source copied as 2560px/**131KB**. Existing ~176MB of oversized assets: a re-upload-in-place migration (same public_id + invalidate keeps URLs working) is the recommended follow-up, NOT run (re-encode is irreversible — Peter to green-light).
- **Library picker polish (Peter)**: "(this site)" → "(current)"; native `<select>` replaced with the shared styled **`Select`** component (the agreed CMS-wide dropdown; auto-searchable at 16 sites).
- **Per-section cross-site "Use" (Peter)**: `ImageUploadField` gained a third action **Replace · Remove · Use** — opens an inline styled-Select of the owner's **same-vertical** sibling sites; picking one resolves THE SAME section's image on that site (`fetchCounterpartSectionImage` in `lib/useSiteMedia.ts`: same page slug + section_type, first by display_order → media-id copy → URL-lookup copy → external URL pass-through) and sets the field automatically — no library browsing. Generic via a `useFrom` locator prop; **wired on the Hero editor** (`{pageSlug, 'hero', image_url, image_media_id}`); other section editors are a one-line add each. Verified live: Ironclad → Use → 3 CrossFit siblings only → Blackfly → field auto-filled with a fresh `ironclad-athletics/…` copy + dirty save bar. Owner still saves (no auto-write).
- **🔎 Finding — demo clones SHARE media references**: rourke's hero content carries argyle's exact URL+media_id; ironclad+blackfly both reference `forge-and-bell/0aa204cb…`. `cloneSite` copies content but NOT media rows/assets — the cross-site delete-trap the copy design guards against exists fleet-wide today (deleting a source asset breaks its clones' pages, and the library "In use" scan can't see cross-site usage). Candidate fix: clone-time media duplication, or extend the usage scan fleet-wide. FLAGGED, not fixed.
- **Auth allow-list (Peter's q)**: `http://localhost:5180/**` was ALREADY allow-listed — the earlier bounce was a stale one-time token misdiagnosis. Proven: fresh magic link with `redirectTo` :5180 lands signed-in on the CMS. (`site_url` = localhost:5173; list also carries crm./cms.stemfra.com.)

### Clone media localization — cross-site sharing fixed (2026-07-09 later, LOCAL)
- **New `lib/localizeSiteMedia.js`**: makes a site OWN every Cloudinary asset it references — scans all media-bearing surfaces (sections/services/team/testimonials/theme_settings/pages/posts, text + JSONB shape-agnostically), finds references whose folder ≠ the site's own (handles legacy `sites/<uuid>/` folders + transformed URLs like the maison-lune `q_auto:eco` hero videos; skips `stemfra_assets`), copies each into the site's folder (images normalized to capped WebP, videos as-is) + inserts site_media rows, then rewrites references in place (key-with-ext → bare-key → media-row-id replacement; old version prefixes on rewritten URLs are Cloudinary-tolerated, verified 200). Idempotent; `{dryRun}` surveys only; dead sources (already-deleted assets) are skipped with a warning, never fail the site.
- **Wired into BOTH provisioning paths** (`provisionSite` + `cloneSite`): `scheduleLocalizeSiteMedia(newSiteId)` fires-and-forgets after the clone commits — provisioning latency doesn't grow; the new site renders on shared refs for the seconds the copies take, then self-heals. (Known tiny race: an owner edit during that window could be overwritten — acceptable, owner hasn't logged in yet.)
- **Fleet backfill run** (`scratchpad/localize_fleet.js`, dry survey → live): 5 sites carried 41 cross-site references — 212/blackfly/ironclad (2 forge hero images each), rourke-sloane (14 argyle assets incl. an mp4 + legacy-folder refs), vesper-beauty-house (21 maison-lune assets incl. the 3 big hero videos). **39 copied, 22 rows rewritten**; re-survey = fleet clean. Change log: scratchpad `localize_fleet_live.json`.
- **2 dead references discovered** (the delete-trap had ALREADY fired historically): `sites/<argyle-uuid>/f254dbe0…` (argyle + rourke About cta_banner backdrops) and `maison-lune/86836bbe…` (maison-lune + vesper home galleries) — sources deleted from Cloudinary long ago, 4 surfaces silently broken. **Repaired**: cta_banner backdrops → each site's own home hero (walk-42's original intent); dead gallery entries dropped (8→7 images).
- **Unrelated dangling ref cleaned**: ironclad team member "Marcus Levin" photo_url pointed at an asset uploaded+deleted during today's CMS testing (no media row, Cloudinary 404-gif) — nulled so the coach card falls back gracefully.
- **Verified**: ironclad renders 0 broken / 0 foreign imgs, all own-folder; rewritten URLs + copied video HEAD 200.

### "Use" extended to every image field (2026-07-09 latest, LOCAL)
- **`CounterpartLocator` generalized** (`lib/useSiteMedia.ts`): section single-field (hero) + **array items** (`arrayKey`+`index` — position-matched: gallery image 2 ↔ the other site's gallery image 2) + **page mode** (`source:'page'` — site_pages columns like og_image_url).
- **Wired**: Gallery (per image), RichText story image, ServiceGrid category-card photos (per card), PageSeo social-share image — plus Hero from earlier. **Empty fields too**: the empty "Add an image" tile now shows a "Use this image from another of your sites" link (the Use panel moved out of the has-value branch), so a fresh gallery slot can pull from a sibling without uploading.
- **Deliberately NOT wired**: entity photos (services/team/testimonials/blog covers) — rows across sites are different people/things with no stable counterpart; the Library site picker covers cross-site reuse there. Branding logo/favicon skipped (brand-specific).
- **Verified live**: 212's gallery shows 6 per-image Use buttons; position-match returns the identical value on identical clones (correct no-op); argyle gallery img 2 ← rourke = real copy (`argyle-and-sons/21db9920…webp`, 3.6s) via the array resolver. Typecheck clean; test copy deleted; drafts discarded.

### Unsplash → Cloudinary migration, fleet + code (2026-07-09 night, LOCAL)
- **Peter's ask**: no hardcoded Unsplash hotlinks anywhere in templates/themes — everything on Cloudinary, per-site where the data is per-site.
- **DB content (the bulk)**: `localizeSiteMedia` gained **`includeExternal`** — ingests `images.unsplash.com` URLs into the site's OWN Cloudinary folder (capped WebP + site_media row) and rewrites the exact URL in place. Enabled for clone-time scheduled runs too (a clone of a site carrying stock hotlinks starts self-contained). Fleet run: **~370 stock images ingested across all 16 demo sites** (~340 rows rewritten); re-survey + SQL count = **zero Unsplash references** left in sections/services/team/testimonials/posts/pages. 2 more dead hotlinks surfaced (photo removed from Unsplash): team member "Marco Rivera" photo on lila-studio/lull/wildflower → nulled (initials fallback).
- **Code constants (10 files)**: uploaded the 10 distinct hardcoded images to SHARED folders (code is shared by all sites on a template, so per-site would be wrong) — `stemfra_assets/templates/` (gift-band, footer-wellness ×3 layouts, footer-barber-tools, 3 spa gift tiers) + `stemfra_assets/cms/` (login hero + 4 signup vertical backdrops) — and patched barbers/yoga/spa/massage Layouts, spa+massage GiftBand, spa GiftCertificatesPage, CMS LoginPage/SignupPage. The only remaining "unsplash" string in platform code is PricingSectionEditor's placeholder EXAMPLE text (format documentation, not a rendered image — left deliberately). All 5 touched workspaces typecheck clean.
- **Verified live**: aurea (heaviest, 44 refs) renders 31/31 images from `aurea/…`, 0 unsplash, 0 broken; argyle full-page 0 unsplash + the new footer-barber-tools texture renders.
- **All 10 dev servers restarted** (4000 backend + cms/client/crm/crossfit/barbers via preview + salons/yoga/massage/spa via npm bg) after Peter's machine restart; ⚠️ the tmp scratchpad (incl. the localize change logs) was wiped by the restart — scripts recreated; the migration itself is durable (DB + Cloudinary).

### P10 kickoff: dispositions applied + Case 4 staff mode SHIPPED (2026-07-10)
- **Peter's dispositions executed**: `verticals` pricing UNIFIED fleet-wide (build 100000 / monthly 9900 — the legacy per-vertical prices fed REAL billing amounts in publishController + admin Stripe checkout); yoga un-flagged (themes.js + verticals.js — Wren Yoga card live w/ hero-fold + CTAs); annual discount threaded (Pricing.jsx reads catalog `annual_discount_months`, helpers take a param, constant = fallback). All verified in preview.
- **P10 planning documented**: `docs/P10_CASES.md` (Peter's 11 cases verbatim + agreed plans) + ROADMAP items 37–45 + status refresh. Build order: Case 4 → 2 → 3 → 7+11 → 1 → 6-R1 → 5(PWA).
- **Case 4 — staff mode in the CMS — SHIPPED + VERIFIED** (see P10_CASES.md implementation notes): server `getStaffInfo` + `verifySiteOwnership` staff bypass w/ deduped `staff_cms_access` audit; CMS `useOwnerContext` staff branch (all 16 sites, synthetic contact, `isStaff`) + amber banner. Verified as dev@stemfra.com: banner, 16-site picker, real hero edit+save+revert on argyle, server 200s on non-owned sites, 3 audit rows landed.
- **Case 2 — CMS theme studio + plans display — SHIPPED + VERIFIED** (same day): ThemesSection cards show live-demo hero-folds (starters + mockups public APIs, matched by theme display_name; subdomain from previewUrl) + "Preview live site ↗"; Billing ChangePlanCard → marketing-style tier cards from `usePublicPlans()` (`/api/plans` — same catalog as the pricing page: promises, live-only features, Most-popular badge). Verified as staff on argyle (theme cards) + lull (plan cards w/ active sub). CMS typecheck clean. NB: fleet rename confirmed — calm-roots-massage IS `lull` now (site 9a505f8e).

## 2026-07-10 — Case 3: promo banner/popup catalog (P10)

- Built the full promotions system: `site_promotions` table (RLS anon/owner/staff),
  11-style token-driven catalog (`promotionStyles.ts` in site-data), archetypes
  `Promotions/` (popup incl. lead-capture form → /api/site-forms/lead, top bar,
  cookie-consent bar, PromotionSurface etiquette host, SitePromotions wrapper),
  mounted in ALL 6 template Layouts before the footer; CMS `/promotions` editor
  (visual style picker + schedule + On/Off) in the Website nav + GlobalSearch.
- Verified end-to-end on argyle: popup 2.5s delay → lead form POST 201 → site_leads
  row → auto-close; cookie accept persists; once-per-session; CMS CRUD as staff.
  Demo promos (split-lead + cookie-bar) left seeded on argyle for review.
- All 6 template apps + CMS typecheck clean. Details: docs/P10_CASES.md Case 3 notes.
- NOT committed (standing rule). Next per the agreed order: Cases 7+11 (domains + email).

## 2026-07-10 — Cases 7+11: CF zones at purchase + email forwarding (P10)

- **Email decision (Peter):** v1 = Cloudflare Email Routing forwarding, free with every
  Stemfra-registered domain; Google Workspace concierge = future; Titan white-label
  mailboxes = parked v2 revenue product. Market survey in P10_CASES.md Case 11.
- **Built:** lib/cloudflareZones.js + lib/domainZone.js (shared zone orchestrator:
  create zone → Porkbun updateNs → apex/www records → enable Email Routing), wired into
  BOTH register paths (cms + admin); porkbun.js gained updateNameServers;
  /api/cms/site-email (status/create/delete aliases, account-destination privacy
  filtering); CMS Settings → Domain "Email forwarding" card with all 4 states.
- **Verified:** server files syntax-clean + nodemon reloaded; CMS typecheck clean;
  preview shows the card + sidebar child + the real 409 no-domain state on argyle.
- **⛔ Live e2e blocked on Peter:** (1) CF API token needs Zone/DNS/Email-Routing edit
  permissions (exact list in P10_CASES notes); (2) Porkbun verification + balance.
- NOT committed (standing rule). P10 next: Case 1 (CMS ease-of-use).

## 2026-07-10 — Case 1: click-to-edit preview + plain-language pass (P10)

- Preview bridge shipped: site-data `previewBridge.ts` (hover outline + "Edit
  section" chip + click→postMessage, scroll-to-section listener) wired into all 6
  templates (main.tsx init + data-stemfra-section tags on the home section loops);
  CMS side: ?stemfraEdit=1 on preview URLs, message listener opens the section's
  editor, expanding an editor scrolls the preview (livePreview.requestPreviewScroll
  + iframe ref in LivePreviewPanel). Verified BOTH directions live on argyle.
- Plain-language pass: Eyebrow→"Small label" (13 editors, hints added), Footer CTA→
  "Bottom button", Meta description→"SEO description". All 7 apps typecheck clean.
- Deferred per plan: live-as-you-type drafts, docs/videos, usability sessions.

## 2026-07-10 — Case 6 R1: variant registry + Components browser (P10)

- lib/variantRegistry.js (14 archetypes × 104 variants, register + description,
  wellness variants included — the doc's tables were stale) + GET
  /api/admin/theme-registry (PLATFORM_OPS; live usage from templates + Starter demo
  URLs) + CRM Marketing → Components tab (filter by archetype/register/search/unused,
  used-by chips, View-live links, dead-code flags). Verified live in the CRM as
  dev@stemfra.com. Screenshots-per-variant swapped for live demo links (deliberate).
- Also this turn: reviewed Peter's new CF token (keep 5 rows, drop Workers KV/Scripts,
  AI Crawl Control, Workers Routes; All accounts + All zones correct). Porkbun funded.
- NOT committed. P10 remaining: Case 5 (PWA), Case 6 R2/R3, live e2e of Cases 7+11
  once the new CF token lands in .env.

## 2026-07-10 — housekeeping: token probe, Pages audit, Case 5 closed

- CF token re-probed after Peter's in-place permission update: zone-create ✓ (probe
  zone deleted), routing rules ✓, destinations ✓; Zone Settings still missing (needed
  by routing enable/status — flag to add; email status endpoint made resilient).
- Pages audit: ALL 7 projects exist, env complete, deploys green (2026-07-07 push).
  Closed the stale "create massage/spa Pages projects" + "verify Template Pages env"
  items in ROADMAP.md.
- Case 5 CLOSED — Peter: no PWA/store apps. Live domain-purchase e2e deferred.

## 2026-07-10 — smaller queue: n8n verticals, Docker/Playwright, demos live, newsletter

- **Email-forwarding reply answer (Peter's Q):** replies come from the owner's personal
  Gmail by default; Gmail "Send mail as" (smtp.gmail.com + app password) fixes it —
  follow-up: add include:_spf.google.com to zone SPF at provisioning + a help note in
  the CMS Email card.
- **n8n vertical sync:** lead-gen system prompt + v11 workflow export updated — six
  verticals (massage + spa in, boutique gyms out), vertical enum {…, massage, spa},
  matched_template += wellness. ⚠️ PETER: paste the updated
  `n8n-workflows/leadgen-system-prompt.txt` into the "Score & Draft (Agent)" node's
  system prompt in n8n (established convention). Server + CRM lists were already in sync.
- **Docker/Playwright:** Dockerfile alpine → node:22-bookworm-slim + chromium via
  `npx playwright install --with-deps` (PLAYWRIGHT_BROWSERS_PATH=/ms-playwright,
  node-fetch healthcheck); deploy.yml += MOCKUP_RENDER_URL=https://crm.stemfra.com.
  Takes effect on the next (held) push.
- **Wellness demos LIVE:** lull/reveline/umbra (massage) + aurea/ellaris-spa/vela (spa)
  published via publishSite({skipBilling}) — all serve 200 at {sub}.stemfra.com.
  Fixed cloudflarePages attachCustomDomain idempotency to also match CF 8000018
  "already added" (hosts were pre-attached 2026-07-07 and re-attach was erroring).
- **Newsletter backend shipped + verified e2e:** `site_newsletter_subscribers` table
  (unique site+email, owner read/delete RLS); POST /api/site-forms/newsletter
  (live/previewing gate, silent dedupe, per-IP rate limit); shared
  `useNewsletterForm` hook wired into all 3 newsletter footers (multi-col,
  visit-deep, dark-editorial) with thanks/error states; all 6 Layouts pass
  `submitNewsletterSignup` (new site-data helper); CMS **Subscribers** page
  (Operations nav + search): list, Copy all, remove. Verified: Ellaris footer signup →
  row → CMS list (test row cleaned). All apps typecheck clean. NOT committed.

## 2026-07-10 — CMS image-editing gaps + "Use" coverage audit (Peter's report)

- **Yoga /services images not editable (root cause):** the yoga Classes page renders
  per-discipline `service_grid` sections whose big photo is the section's `image_url`
  content key — added for Manhattan's photo-menu but never given a CMS field. FIXED:
  `ServiceGridSectionEditor` gained a "Section image" ImageUploadField (image_url +
  image_media_id, cleanup + Use).
- **Missing image fields found by the audit + added (all with media tracking + Use):**
  Testimonials backdrop (`backdrop_image_url` — Beauty House marble), Location photo
  (`image_url` — photo-panel/dark-panel), CTA banner photo (`backdrop_image_url` —
  About band / gift bands), and a brand-new **BrandBandSectionEditor** (accent line,
  photo + side — was "Editor coming soon"; registered in EDITABLE_TYPES/labels/switch).
- **"Use" cross-site reuse extended to catalog rows:** `CounterpartLocator` gained
  `source: 'service' | 'team'` (+ `name`; pageSlug now optional) — counterpart = the
  other site's row matched by NAME first (verified 1:1 across the yoga demos), else
  same list position. Wired into ServiceCard, TeamMemberCard AND the draft cards
  (gotcha: the first wiring landed only in DraftServiceCard — the existing-row
  editors are separate components). This closes the Beauty-House case: its tiles
  render `site_services.photo_url`, edited on the Services page.
- Verified in preview on lila-studio: Section image field + Use render on
  /content/services; brand_band shows the real editor; Use renders on the /services
  catalog photo field. CMS typecheck clean. Amenities photo URLs (line-based, Volt
  only) left as-is; testimonial author photos skipped (low value) — both flagged.

## 2026-07-10 — Outreach system rediscovered + A1 injection into System B

- **Peter's question answered:** the prospecting email system (June 29 arc) =
  Template Manager (email_templates A1–A20 + B1–B9, CRM → Email Templates), Mark's
  Gmail identity + open pixel + reply sweeper, the DB-driven drip
  A1→A2(+7d)→read-gated VOICE call(+8d, only if A2 opened + not signed up)→A8(+14d)
  →A20 breakup(+21d), callGuardrails (DNC, 12–18 ET, cap 50), replyClassify;
  automation flags all OFF. Was documented only in WORK_2026-06-29.md + code.
- **NEW system-of-record doc: docs/OUTREACH.md** — consolidated + indexed at
  project level: docs hub (docs/README.md), both LEADGEN.md docs (cross-refs),
  server CLAUDE.md leadgen section, leadgen memory.
- **Drift gaps FIXED (System B ↔ Template Manager):** the scoring agent never read
  A1 and never emitted {{demo_link}}/{{start_free_link}}. Now: /api/leadgen/trigger
  fetches the active A1 → payload.template_a1; new n8n paste file
  `n8n-workflows/leadgen-build-prompt.js` (Build Prompt node) injects it per
  candidate with rules (keep the two send-time merge fields literal, substitute
  real values elsewhere, omit the signature — send-outreach auto-appends Mark's);
  `leadgen-system-prompt.txt` DRAFT paragraph updated to honor the template
  (≤120 words with template, ≤100 freehand). v11 JSON export mirrors both.
  ⚠️ PETER: paste BOTH into the v11 workflow — Build Prompt node jsCode +
  the Agent node system prompt (again — it changed after this morning's paste).
- Stale CLAUDE.md verticals note corrected (n8n synced today).

## 2026-07-10 — Case 9 Phase 1: unified email base + all senders migrated

- templates/baseEmail.js (Hostinger structure + Claude restraint; Stemfra + TENANT
  brand modes) + templates/transactionalEmails.js (7 builders); marketing
  contact pair rebuilt on the base; booking ×3 / owner-lead / chat-lead / Stacy
  handoff / orphan-payment senders now attach branded HTML (text kept).
  /dev/preview extended to 9 sample variants — visually verified in browser.
- Server :4000 crashed on a mid-edit syntax error → repaired + restarted (bg).
- Next Case 9 phases: Supabase auth-email templates + SMTP, login alerts,
  2FA recovery codes; billing dunning builder when System A emails get wired.

## 2026-07-10 — Case 9 Phase 1.5: brand lockups + Hostinger polish + sender research

- **Email base refinements (Peter's review):** tenant header now renders logo +
  business NAME under it (never logo-only); headings + body copy centered (Hostinger,
  aligning with the centered logo); Stemfra-branded CTAs turn violet #6366F1 (CMS
  accent) incl. the numbered steps — tenant mail keeps the neutral dark button;
  standard anti-phishing footer ("If you didn't make this booking, contact {business}
  at {their email} or support@stemfra.com").
- **Booking sends now fetch tenant bits** (new getTenantEmailBits in bookingController):
  site_theme_settings.logo_url + the business's public email (home location_map
  content.email) → branded header, security line, and **replyTo** — "just reply" now
  actually reaches the business, not Stemfra's Gmail.
- **Navbar correction (all 11 Header variants):** a logo no longer REPLACES the
  business name — left-brand navs render logo + name side by side (each variant's own
  type style); centered-wordmark renders logo with the name UNDER it (marketing-page
  style; its mobile bar is row). Verified live on argyle (:5174 — restarted; it died
  with the old preview session). All 6 templates typecheck clean.
- **Sender research (Mindbody/Postmark canon):** platforms send from their OWN
  authenticated domain (Mindbody default noreply@hirefrederick.com), business as
  DISPLAY NAME, custom sender domain = advanced opt-in requiring the business's
  SPF/DKIM/DMARC. Proposal logged in P10_CASES (Phase 5 sender infrastructure +
  action-link booking management).

## 2026-07-10 — Notification catalog research (Mindbody deep-dive)

- Captured Mindbody's FULL Notifications model via the browser (45+ notifications,
  5 categories w/ opt-in semantics, per-notification owner controls incl. reply-to +
  business-copy BCC, Operations = mandatory email-only). NEW DOC:
  **docs/EMAIL_SMS_CATALOG.md** — target catalog vs Stemfra coverage + proposed
  N1–N5 build order (indexed in the docs hub). Headline gaps: appointment REMINDERS
  never send (fields exist, no sweeper), no cancellation/reschedule client emails,
  owner booking notifications are bell-only, zero tenant SMS (A2P 10DLC needed),
  no client notification-preference/unsubscribe model, Stripe receipts not enabled.
- Pending Peter: finalize N1–N5 order + the production-sender discussion.

## 2026-07-10 — Notification plan FINALIZED (N1–N5) + Mindbody checklist dispositioned

- Peter approved N1–N5; ESP = Resend (free plan first; he opens the account —
  onboarding steps in EMAIL_SMS_CATALOG.md "Resend onboarding"). Payment-dependent
  emails BUILD now / ACTIVATE post-Stripe-verification. Airwallex may replace
  Payoneer for System A collection (billing emails stay provider-agnostic; full
  discussion deferred). Google-Workspace-seat savings noted as a side benefit.
- Every notification on Peter's Mindbody checklist dispositioned in
  EMAIL_SMS_CATALOG.md § "Per-item disposition" (✅ shipped / 🔨 build w/ phase /
  💳 Stripe-gated / ⏸ parked-with-feature / ✖ N-A) + phase checklists for the
  end-of-arc review. Tasks #12–#16 track N1–N5.
- NEXT: N1 build (reminder sweeper, cancellation/reschedule emails, owner
  booking notifications, Stripe receipt flag).
