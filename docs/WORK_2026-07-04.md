# Session work log — 2026-07-03 / 2026-07-04

One (long) session, four arcs. **Nothing committed** — the whole session is uncommitted
across `stemfra_server`, `stemfra-ops`, `stemfra_client`, and `stemfra_platform`; a
commit pass is the first follow-up (see ROADMAP P9). Per-arc source-of-truth docs are
linked; this log is the map.

## 1. Marketing Mockups studio — feature-complete locally
**Doc: `stemfra-ops/docs/MARKETING_MOCKUPS.md`** (kept current step-by-step; read that,
not this). Highlights of what shipped this session:
- Per-slot card library (`AssetLibraryModal`): URL paste · device upload · **From demo**.
- **Server capture pipeline** (`stemfra_server` `/api/admin/mockups/*`, new deps
  playwright+sharp): composite capture (2–4×), demo-page screenshots (SSRF-safe
  starterId+path), **prepared 4× tile masters** (render a page once → Cloudinary tiles →
  instant server-stitched crops incl. "Whole page" with 25MP auto-fit), saved mockups on
  demo metadata, public `GET /api/marketing/mockups`.
- **Crop UX = scroll-to-capture viewfinder** (replaced the draggable frame), per-device
  render presets (Chrome-style Device dropdown incl. MacBook Air/iMac/iPhones — CSS px),
  Panel/Fold frame presets, zoom.
- **Two new scenes** from Peter's references: **Page panels** (Aura-X) and
  **Desktop + Mobile** (Escape-Massage) — registry-driven fixed-window panel layouts
  (`PagePanelsScene` + geometry in `MOCKUP_LAYOUTS`); scene tabs trimmed (flat grid +
  board-9 retired); Config Save/Reset baseline; localStorage draft persistence.
- `stemfra_client`: `WorkMarquee` consumes saved mockups by subdomain (fallbacks kept);
  the old `/mockups` builder deleted.
- **Pending**: prod deploy (chromium+sharp in Docker, `MOCKUP_RENDER_URL`, CRM env flip).

## 2. CMS dashboard v3 (stemfra_platform)
**Doc: platform `CLAUDE.md` → "Dashboard v3"**. Two-row KPI layout with **MRR**
(subscriptions normalized by billing interval), month-over-month delta chips, customers
count, 30-day Bookings|Revenue chart with prior-period compare, Top services panel.
Two new hooks in `lib/queries.ts`; zero new deps. Verified as Marcus (payments-off
states) + Davis (real $325 MRR). Honest gap: "Revenue this month" = paid bookings only
(membership charge history needs a server endpoint — unbuilt).

## 3. Wellness verticals (massage live, spa parked)
**Doc: platform `CLAUDE.md` → "Wellness verticals"** + `project_wellness_verticals`
memory. Pillars = Beauty · Fitness · Wellness (replaces boutique_gyms). Massage
scaffolded end-to-end: DB verticals/templates rows, seed `calm-roots-massage` (via
`onboardCustomer` clone of lila-studio — generated UUID, not a sentinel), app
`stemfra_massage` (port 5181), wiring (root scripts, launch.json, server CORS,
`verticalConfig` — boutique_gyms removed), CRM lead-gen dropdown + CMS domain map.
**Spa deliberately parked** (Peter's sequencing: build from the completed massage).
CMS fixture login: noa@calm-roots-massage.com / calmroots-dev-2026.

## 4. Massage Theme 1 — "Escape" (massage-escape)
**Docs: platform `CLAUDE.md` → "Massage Theme 1"** + `THEME_VARIANTS.md` (registered) +
the spec at `stemfra_design_system/massage/Massage_Theme_1/massage-escape.spec.md`.
Built per THEME_SYSTEM §7: v1 data-only (tokens cross-checked vs the design-guide HTML,
all-reused variants, full seed-content pack in the Escape voice, `theme-audit` green),
then the **structure pass** matching the reference webp: NEW `hero:overlay-booking`
archetype (photo tile strip + floating LIVE booking bar — real service select,
duration/price read-outs, `?service=` link), app announcement strip
(`theme metadata.announcement`), NEW `intro` layout `photo-overlay` (the "Rejuvenate"
band). Remaining 🟡: process-steps, diamond-split, single-rate-panel, per-section
`backgrounds` painting.

## Cross-repo doc updates made this session
- `stemfra_server/CLAUDE.md` — new endpoints/deps/CORS/verticals sections (2026-07-04)
- `stemfra-ops/CLAUDE.md` — Marketing Mockups section rewritten; lead-gen vertical note
- `stemfra-ops/docs/LEADGEN.md` — vertical list + allow-list derivation corrected
- `stemfra_platform/CLAUDE.md` — Wellness verticals + Dashboard v3 + Massage Theme 1
  sections; CMS test-logins table (+Noa)
- `stemfra_platform/docs/THEME_VARIANTS.md` — massage-escape registered (+2 new variants)
- `docs/ROADMAP.md` — new **P9** (this session's follow-ups)
